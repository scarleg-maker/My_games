# Dépendances Node.js (régénérées via npm install, jamais à uploader)
node_modules/

# Fichiers d'environnement (clés secrètes, à ne jamais publier)
.env
.env.local

# Fichiers système
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Fichiers temporaires de build
dist/
build/
